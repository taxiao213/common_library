<resources>
    <!-- Base application theme. -->
    <style name="LibraryCommonAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--没有标题栏-->
        <item name="android:windowNoTitle">true</item>
        <!--窗口界面颜色-->
        <item name="android:windowBackground">@color/colorWindowBg</item>
        <!--状态栏背景颜色-->
        <item name="colorPrimaryDark">@android:color/transparent</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--dialog动画-->
    <style name="LibraryBottomDialogAnimation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/library_anim_dialog_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/library_anim_dialog_bottom_out</item>
    </style>

    <!--calendarDialogTheme-->
    <style name="LibraryCalendarDialogTheme" parent="Theme.AppCompat.Dialog">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@null</item>
        <item name="android:backgroundDimEnabled">true</item><!--背景模糊-->
    </style>

    <!--calendarDialogTheme-->
    <style name="LibraryCalendarDialogThemeNobg" parent="Theme.AppCompat.Dialog">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:background">@null</item>
        <item name="android:backgroundDimEnabled">false</item><!--背景模糊-->
    </style>

    <!--分割线-->
    <style name="LibrarySplitLine">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@color/colorWindowBg</item>
    </style>

    <!--分割bar-->
    <style name="LibrarySplitBar">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">6dp</item>
        <item name="android:background">@color/colorWindowBg</item>
    </style>

</resources>